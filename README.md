# Jonte Test — Hello World

A single-page "Hello World" experience that looks great on mobile and desktop. Built with semantic HTML and responsive CSS—no build tooling required.

## Preview

Open `index.html` in any browser or run a tiny static server:

```bash
python3 -m http.server 4173
```

Then visit <http://localhost:4173>.

## Features

- 📱 Mobile-first layout (fluid spacing, clamp-based typography)
- 🌫️ Glassmorphism card with animated sparkles
- ✨ "Say hello" button to prove it works
- 🪶 Zero dependencies
